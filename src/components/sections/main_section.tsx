// pages/about.tsx

import { FC } from "react";
import { useSession } from "next-auth/react";

const Main: FC = () => {
  return (
    <div className="flex flex-1 items-stretch overflow-hidden mt-[64px] md:ml-[112px]">
        Main
    </div>
  );
};

export default Main;
